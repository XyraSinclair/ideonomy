[project]
name = "ideonomy"
version = "0.1.0"
description = "Computational ideonomy: inference-time cognitive building blocks (the organon)"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["ideonomy*"]
